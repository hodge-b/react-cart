import { render } from "@testing-library/react";
import Customers from "./Customers";

describe("Customers", () => {
  it("renders the Customers component", () => {
    render(<Customers />);
  });
});
